import {makeStyles} from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    logo: {
        textAlign: 'center',
    },
    input: {
        width: '100%',
        marginTop: '10px',
    },
    esqueciMinhaSenha: {
        marginTop: '10px',
        float: 'right',
    },
}));
